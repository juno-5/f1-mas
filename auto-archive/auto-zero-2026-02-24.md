# Auto×Zero — 2026-02-24

Continuing from Cycle #40 (2026-02-23).

---

## Cycle #41 — Hot Reload Empty Index Guard

**[관찰]**
- 51 total requests, 6 failed (11.8% failure rate)
- 3 failures: "no suitable personas found" — infra/NAS queries
  - `87da286a` (11:15:18): "서버 상태 확인해줘" — domain=developers, functions=[]
  - `0e9c38f9` (09:33:07): "현재 연결된 노드 PC의..." — domain=developers, functions=[]
  - `8c4916c1` (09:32:38): same query — domain=developers, functions=[]
- **Transient**: same queries succeeded 1 min before and 1 min after
- **Race condition evidence**: at 11:15:18, TWO identical requests — one completed, one failed

**[심층 분석]**
1. 현상 분해: `_select_by_function()` has domain fallback (line 262) that should ALWAYS return personas. Failures were transient, not systematic.
2. 근인 추적: `load()` clears ALL index dicts (line 181-185) BEFORE repopulating. If `entries` is empty (parsing failure from corrupt/partial file during SCP), index becomes empty until next hot reload (30s).
3. 구조적 패턴: Clear-then-populate pattern is vulnerable to empty results. Entries are parsed OUTSIDE the lock, but clear happens INSIDE — empty entries still trigger clearing.
4. 레버리지 포인트: Guard in `load()` prevents clearing when entries is empty; guard in `_load_functions_yaml()` prevents caching empty patterns.

**[가설]**
- Claim: "no suitable personas found" caused by hot reload clearing index during SCP deployment
- Basis: (a) `load()` clears dicts even when entries=[] (b) Same query succeeded before/after (c) Simultaneous requests had different outcomes
- Prediction: Guard prevents all transient persona selection failures
- Null: Failures recur with guard in place
- Risk: low — guard only skips when entries empty AND index already loaded
- Impact: Tier 1 reliability — eliminates 50% of all failures (3/6)

**[실험 설계]**
- 실험: Add empty-guard to `load()` + no-cache-empty to `_load_functions_yaml()`
- 채택: Both queries ("서버 상태 확인해줘", "노드 PC hostname CPU") succeed post-deploy
- 기각: Either query still fails with "no suitable personas found"
- 범위: mas_persona_index.py (2 locations)
- 되돌림: 가능 — guard only adds early return

**[실험 결과]**
- `서버 상태 확인해줘` → Sentinel (F1-11) selected, completed (2,118 chars) ✅
- `노드 PC hostname CPU` → Sentinel (F1-11) selected, running ✅
- No WARN logs during normal operation (guard not triggering spuriously) ✅

**[검증] ✅ Confirmed**
- 예측: Guard prevents empty index from being used
- 실제: Both previously-failing queries succeed, no false warnings
- 설명: The guard adds a safety net against a structural vulnerability in hot reload

**[적용]**
- `mas/mas_persona_index.py`: +12 lines
  - `load()`: skip clear if entries empty and already loaded (line 182-185)
  - `_load_functions_yaml()`: don't cache empty compiled, return for retry (line 980-984)
- Deployed via SCP, restarted MAS, tested

**[아카이브]**
- auto-zero-2026-02-24.md 업데이트 완료
- auto-zero-summary.md 업데이트 완료
- git commit: `b12b3c7`

→ 다음 가설: domains에 'sales' 오탐 조사, 또는 `Functions: []` field가 state에 저장되지 않는 문제

---

## Cycle #42 — Haiku Synthesis Model Cost Optimization

**[관찰]**
- 53 total requests, 45 completed — synthesis cost avg $0.072/req using sonnet
- Total synthesis spend: ~$2.11 (21.6% of total $9.76)
- Synthesis is simple summarization task — agents already did the deep thinking
- Config already supports `synthesis_model` field (line 813 in `mas_agent_runner.py`)
- Sonnet pricing: $3/$15 per MTok (input/output). Haiku: $0.80/$4.00

**[심층 분석]**
1. 현상 분해: Synthesis takes agent outputs (2-5 agents × ~3-10K chars each) and creates a unified summary. This is a well-structured summarization task with explicit template (Key Agreements, Tensions, Unified Recommendation, Action Items).
2. 근인 추적: Synthesis uses the same model as agents (`claude_model: sonnet`) because `synthesis_model` was set to "sonnet" by default.
3. 구조적 패턴: Over-provisioning pattern — using expensive model for routine task. Synthesis doesn't need creative reasoning or deep analysis; it reorganizes pre-computed outputs.
4. 레버리지 포인트: Single config change (`synthesis_model: haiku`) affects ALL multi-agent requests (30/53 = 56% of requests).

**[가설]**
- Claim: Using haiku for synthesis maintains quality while reducing synthesis cost by >70%
- Basis: (a) Synthesis is structured summarization with explicit template (b) Input content already contains analysis — synthesis just reorganizes (c) Haiku handles summarization well
- Prediction: Haiku synthesis produces comparable quality (structured sections, proper tables, actionable items) at <$0.025/synthesis
- Null: Haiku output is significantly worse (missing sections, wrong conclusions, poor formatting)
- Risk: low — single config revert to restore sonnet
- Impact: Tier 3 efficiency — saves ~$2/day (21% cost reduction on synthesis portion)

**[실험 설계]**
- 실험: Change `synthesis_model` from "sonnet" to "haiku" in runtime config
- 채택: Synthesis output has all required sections, quality comparable to sonnet, cost <$0.025
- 기각: Output quality noticeably degraded or missing expected sections
- 범위: `~/.f1crew/shared/mas-config.json` (runtime) + `config/mas-config.json` (source)
- 되돌림: 가능 — config-only change

**[실험 과정 — 부수 발견]**
- **Config path mismatch discovered**: MAS reads from `~/.f1crew/shared/mas-config.json` (line 11, `mas_config.py`), NOT from project source `config/mas-config.json`
- Initial SCP deployed to wrong path → first test (10ffcd0b) used sonnet despite source config change
- Corrected: updated runtime config directly via SSH

**[실험 결과]**
- Test query: "React와 Vue.js의 상태관리 패턴을 비교 분석해줘. Redux vs Pinia 중심으로."
- 2 agents: Blaze ($0.0799, 6083 chars) + Map ($0.1058, 6976 chars)
- **Haiku synthesis**: 18,271 tokens, **$0.0182**, 57,176ms, 3,457 chars
- Output quality:
  - Key Agreements section ✅ (3 items with table)
  - Tensions section ✅ (3 tension points with balanced analysis)
  - Unified Recommendation ✅ (decision framework with checklist + table)
  - Action Items ✅ (3 priority levels, 8 items)
  - Team Insight ✅
  - Proper markdown formatting with tables, code blocks, headers ✅

**[검증] ✅ Confirmed**
- 예측: Cost <$0.025 with comparable quality
- 실제: Cost $0.0182 (75.8% reduction from $0.075 avg), quality fully maintained
- 설명: Haiku excels at structured summarization when given well-organized input. The synthesis template provides clear structure, and agent outputs provide the content — haiku just needs to reorganize.

**[적용]**
- `~/.f1crew/shared/mas-config.json` (runtime): `synthesis_model` "sonnet" → "haiku" (직접 수정)
- `config/mas-config.json` (source): already "haiku" from previous session
- Performance record confirms `model: haiku` in JSONL

**[아카이브]**
- auto-zero-2026-02-24.md 업데이트 완료
- auto-zero-summary.md 업데이트 완료
- git commit: pending

→ 다음 가설: Config path 이중성 문제 (source vs runtime), 또는 MAS 빈번한 재시작 원인 분석

---

## Cycle #43 — Metadata Prefix Stripping for Detection Accuracy

**[관찰]**
- Request `3f4ad732`: "[auto-debug] Cycle #24: 모바일 앱 결제 UX 개선 전략..."
- Functions detected: `['debugging', 'payment_checkout', 'mobile_ux']`
- `debugging` false positive: "debug" in `[auto-debug]` prefix matches `디버그|debug` pattern
- Selection: Touch (UIUX) + Trace (debugger) — should have been Touch + Apex (payment)
- The `[auto-debug]` prefix also polluted domains: `developers` detected from "debug"

**[심층 분석]**
1. 현상 분해: `analyze_request()` passes raw query including metadata prefixes to all detection functions. Prefixes like `[auto-debug]` contain keywords that trigger false function/domain matches.
2. 근인 추적: No query preprocessing step exists. Any text in the query is treated as user content.
3. 구조적 패턴: Missing content/metadata boundary awareness in text-based detection pipeline.
4. 레버리지 포인트: Single preprocessing step strips known metadata patterns before detection. Affects all prefixed queries.

**[가설]**
- Claim: Stripping `[auto-*]` metadata prefixes before detection eliminates prefix-based false positives
- Basis: `[auto-debug]` prefix caused `debugging` false positive, displacing `payment_checkout`
- Prediction: Same query selects payment specialist instead of debugger
- Null: No change in selection
- Risk: low — regex only matches `[auto-*]` format, leaves normal brackets untouched
- Impact: Tier 2 agent intelligence — correct persona selection for all auto-session queries

**[실험 설계]**
- 실험: Add `_METADATA_PREFIX_RE` strip in `analyze_request()` before detection calls
- 채택: Same query detects `['payment_checkout', 'mobile_ux']` without `debugging`
- 기각: `debugging` still detected
- 범위: `mas/mas_orchestrator.py` (3 lines changed + 5 lines regex)
- 되돌림: 가능 — remove 8 lines

**[실험 결과]**
- A/B test with same query:
  - **With prefix (before fix)**: functions=`['debugging', 'payment_checkout', 'mobile_ux']`, selected Touch + Trace
  - **With prefix (after fix)**: functions=`['payment_checkout', 'mobile_ux']`, selected Touch + Apex ✅
  - **Without prefix**: functions=`['payment_checkout']`, selected Apex ✅
- External validation: auto-debug Cycle #25 query (YouTube content) no longer has `debugging` in functions ✅
- Regex correctly preserves `[긴급] 서버 상태 확인해줘` — only strips `[auto-*]` format ✅

**[검증] ✅ Confirmed**
- 예측: prefix stripping eliminates `debugging` false positive
- 실제: functions list changed from 3 to 2, persona selection switched from Trace→Apex
- 설명: Single regex strip removes metadata noise from detection pipeline. No impact on non-prefixed queries.

**[적용]**
- `mas/mas_orchestrator.py`: +8 lines
  - `_METADATA_PREFIX_RE`: regex for `[auto-*] Cycle #N:` pattern
  - `analyze_request()`: strip prefix before domain/function/locale detection
  - `query_len` now uses `clean_query` for accurate complexity estimation
- Deployed via SCP, restart pending (active requests)

**[아카이브]**
- auto-zero-2026-02-24.md 업데이트 완료
- auto-zero-summary.md 업데이트 완료
- git commit: pending

→ 다음 가설: Library injection domain mismatch (developers injected for creatives query), 또는 xapi 빈번한 재시작 원인 분석

---

## Cycle #44 — Global Locale Matching for Five Senses Personas

**[관찰]**
- YouTube branded content query → Selected: Yena Jang + Resonance (marketer + developer)
- Expected: CHROMA (color) + ECHO (sound) + Yena Jang (design)
- Five Senses personas (01-05) all have `locale=global`
- Korean queries have `locale=korea`
- Locale-matching loop: `if c.locale == locale` → "global" ≠ "korea" → Five Senses never match

**[심층 분석]**
1. 현상 분해: `_select_by_function()` locale preference at line 249-252 only matches exact locale. "global" is treated as a foreign locale.
2. 근인 추적: Five Senses are global by nature (transcend locale). But selection algorithm has no concept of "matches any".
3. 구조적 패턴: Missing wildcard/universal locale handling. Affects all 11 creative personas (5 Five Senses + 6 Art Masters).
4. 레버리지 포인트: Add `or c.locale == "global"` to locale match conditions. 4 locations in orchestrator.

**[가설]**
- Claim: Treating `locale=global` as matching any locale restores Five Senses to their function-priority position
- Basis: ECHO (sound_music priority #1) systematically loses to Resonance (priority #2) due to locale mismatch
- Prediction: Creative queries select Five Senses + domain designers instead of cross-domain substitutes
- Null: No change in creative query selections
- Risk: low — "global" personas become locale-neutral, which matches their intent
- Impact: Tier 2 agent intelligence — 11 creative personas restored to proper selection priority

**[실험 결과]**
- Test: "유튜브 브랜디드 콘텐츠 — 비주얼/컬러/사운드 디자인"
  - **Before**: Selected `['Yena Jang', 'Resonance']` (marketer + developer)
  - **After**: Selected `['CHROMA', 'ECHO', 'Yena Jang']` (color creative + sound creative + designer) ✅
- All 3 agents completed, haiku synthesis $0.0225, 3,402 chars ✅
- Fix applied to all 4 locale-matching locations in orchestrator

**[검증] ✅ Confirmed**
- 예측: Five Senses selected for creative queries
- 실제: CHROMA + ECHO selected (both global locale, now matching korea)
- 설명: `locale=global` is now treated as a universal match, restoring function-priority order

**[적용]**
- `mas/mas_orchestrator.py`: 4 locale-matching locations updated
  - `_select_by_function()` Pass 1: line 250
  - `_select_from_pool()` Pass 1: line 176
  - `_select_from_pool()` Pass 2: line 187
  - Fallback domain selection: line 277
- Deployed via SCP, MAS restarted, tested

**[아카이브]**
- auto-zero-2026-02-24.md 업데이트 완료
- auto-zero-summary.md 업데이트 완료
- git commit: pending

→ 다음 가설: Five Senses 선택 후 library injection이 creatives 도메인으로 올바르게 주입되는지 확인

---

## Cycle #45 — Library Injection Domain Verification for Five Senses

**[관찰]**
- Cycle #44에서 Five Senses 선택 복원 후, library injection이 올바른 도메인으로 주입되는지 검증 필요
- Request `9ff0205d`: "유튜브 브랜디드 콘텐츠 시리즈 기획 — 비주얼/컬러/사운드 디자인"
- Selected: CHROMA (02) + ECHO (03) + Yena Jang (DES-KR-01)
- MAS 로그에서 library injection 확인:
  - `Library: injecting 2748 chars for domain=creatives` (CHROMA)
  - `Library: injecting 2748 chars for domain=creatives` (ECHO)
  - `Library: injecting 6051 chars for domain=marketers` (Yena Jang)

**[심층 분석]**
1. 현상 분해: `_build_agent_prompt()` line 276-277에서 `library_context = _fetch_library_context(domain or persona.category)`. `_execute_parallel()`이 `domain` 미전달 → `persona.category` 사용.
2. 근인 추적: Five Senses `persona.category = "creatives"` → `_CATEGORY_DOMAIN_MAP["creatives"] = "creatives"` → `library/creatives/` 참조.
3. 구조적 패턴: 각 페르소나가 자신의 도메인 라이브러리를 독립적으로 가져오는 설계. 혼합 도메인 팀에서도 정확하게 작동.
4. 레버리지 포인트: 코드 수정 불필요 — 설계가 올바름.

**[가설]**
- Claim: Five Senses 선택 시 `library/creatives/` 라이브러리가 올바르게 주입된다
- Basis: `_build_agent_prompt()`이 `persona.category` 기반으로 라이브러리 선택
- Prediction: 로그에서 `domain=creatives` 주입 확인
- Null: 다른 도메인 라이브러리가 주입됨
- Risk: low — 읽기 전용 검증
- Impact: Tier 2 에이전트 지능 — 올바른 도메인 지식 주입 확인

**[실험 결과]**
- Request `9ff0205d` 로그 분석:
  - CHROMA (category=creatives) → `domain=creatives`, 2748 chars ✅
  - ECHO (category=creatives) → `domain=creatives`, 2748 chars ✅
  - Yena Jang (category=marketers) → `domain=marketers`, 6051 chars ✅
- 3 agents 전원 completed, synthesis haiku $0.0225 ✅
- `library/creatives/references.md`: 3686 bytes (AI 도구, 프롬프트 가이드, 프로덕션 리소스)
- `library/creatives/insights.md`: 183 bytes (헤더만, 실제 인사이트 미축적)

**[검증] ✅ Confirmed**
- 예측: Five Senses → creatives 라이브러리 주입
- 실제: 정확히 예측대로 작동. 혼합 도메인 팀에서도 각 페르소나 올바른 도메인 라이브러리 사용
- 설명: `persona.category` 기반 라이브러리 선택이 올바르게 설계됨. 코드 수정 불필요.

**[부수 관찰]**
1. creatives 라이브러리(2748 chars) — 다른 도메인(6050+ chars)의 절반 미만
2. creatives `insights.md` 비어있음 — 아직 creatives 도메인 인사이트 미축적
3. 오늘 21 requests, 6 agent failures (15.4%), 2 synthesis failures — 모두 xapi 다운타임 관련
4. Synthesis 비용 haiku 전환 후 $0.020/synthesis avg (Cycle #42 일치)
5. 메타데이터 접두사가 AMM fetch에는 여전히 포함됨 — 검출에만 strip, 실행 파이프라인에는 미적용

**[아카이브]**
- auto-zero-2026-02-24.md 업데이트 완료
- git commit: pending

→ 다음 가설: 메타데이터 접두사를 분석뿐 아니라 실행 파이프라인에도 전파하여 AMM/에이전트 프롬프트 노이즈 제거

---

## Cycle #46 — Clean Query Propagation to Execution Pipeline

**[관찰]**
- Cycle #43에서 `analyze_request()`에 메타데이터 접두사 strip 추가 — 검출 정확도 개선 완료
- 하지만 실행 파이프라인에는 원본 query 전달됨:
  - AMM fetch: `[auto-debug] Cycle #28: React Native 앱에서 FlatList...` (접두사 포함)
  - 에이전트 프롬프트: 접두사가 Task 섹션에 포함
  - Synthesis prompt: 접두사가 원본 쿼리에 포함
- 접두사는 비콘텐츠 메타데이터 — AMM 검색 관련성 저해, 에이전트에 불필요한 노이즈

**[심층 분석]**
1. 현상 분해: `analyze_request()`에서 `clean_query` 생성하지만 반환 dict에 미포함. `execute()` → `execute_pattern()` → `_execute_parallel()` → `_build_agent_prompt()` → AMM/NAS/Library/Prompt 전체가 raw query 사용.
2. 근인 추적: Cycle #43 설계 시 검출(detection) 정확도에만 집중. 실행 파이프라인의 쿼리 품질은 고려하지 않음.
3. 구조적 패턴: 전처리 결과가 분석 단계에 갇혀있고, 실행 단계로 전파되지 않음.
4. 레버리지 포인트: `analyze_request()` 반환 dict에 `clean_query` 추가 → `execute()`에서 추출 → `execute_pattern()`에 전달. 2줄 수정으로 전체 파이프라인에 전파.

**[가설]**
- Claim: `clean_query`를 실행 파이프라인에 전파하면 AMM, 에이전트 프롬프트, synthesis 모두 노이즈 없는 쿼리 사용
- Basis: `execute_pattern(query=...)` 하나의 진입점으로 모든 downstream 함수에 전파됨
- Prediction: AMM fetch 로그에 `[auto-*]` 접두사 없이 깨끗한 쿼리 표시
- Null: AMM fetch에 여전히 접두사 포함
- Risk: low — raw query는 Slack 알림과 state에 유지, 실행만 clean_query
- Impact: Tier 2 에이전트 지능 — AMM 검색 관련성 향상 + 에이전트 프롬프트 노이즈 제거

**[실험 설계]**
- 실험: `analyze_request()` return dict에 `clean_query` 추가, `execute()`에서 `execute_pattern()`에 전달
- 채택: AMM fetch 로그에 접두사 없는 쿼리 표시
- 기각: AMM fetch에 여전히 접두사 포함
- 범위: `mas/mas_orchestrator.py` (2 locations, +3 lines)
- 되돌림: 가능 — revert 2 edits

**[실험 결과]**
- Test: `[auto-zero] Cycle #46: React Native 앱에서 FlatList 성능 최적화 방법 알려줘`
- **Analysis log**: `'clean_query': 'React Native 앱에서 FlatList 성능 최적화 방법 알려줘'` ✅
- **AMM fetch (BEFORE)**: `query: [auto-debug] Cycle #28: React Native 앱에서 FlatList...`
- **AMM fetch (AFTER)**: `query: React Native 앱에서 FlatList 성능 최적화 방법 알려줘` ✅
- **Request log (raw preserved)**: `[REQUEST] 7d0de8ee: [auto-zero] Cycle #46: React Native...` ✅
- Blaze selected, completed: 61.3s, 6094 chars, $0.0255 ✅

**[검증] ✅ Confirmed**
- 예측: AMM/에이전트에 접두사 없는 쿼리 전달
- 실제: AMM 로그에서 접두사 제거 확인, raw query는 디버그 로그에 유지
- 설명: `clean_query`를 analysis dict에 추가하고 `execute_pattern()`에 전달하는 것만으로 전체 실행 파이프라인에 전파됨

**[적용]**
- `mas/mas_orchestrator.py`: +3 lines
  - `analyze_request()`: `"clean_query": clean_query` 반환 dict에 추가
  - `execute()`: `agent_query = analysis.get("clean_query", query)` → `execute_pattern(query=agent_query)`
- Deployed via SCP, MAS restarted, tested

**[아카이브]**
- auto-zero-2026-02-24.md 업데이트 완료
- git commit: pending

→ 다음 가설: 오늘 성능 데이터 기반 새로운 최적화 포인트 탐색 — token breakdown recording, 또는 library context 크기 불균형

---

## Cycle #47 — Performance Function Regex Disambiguation

**[관찰]**
- Request `25cd260d`: "Please provide a detailed analysis comparing our brand strategy against competitors performance"
- Functions detected: `['performance', 'brand_strategy']` — `performance` false positive
- `performance` function maps to developer personas (F1-03, FC-08)
- Brand context "performance" != technical performance → wrong persona (Core) added alongside Ashley Yoo

**[심층 분석]**
1. 현상 분해: `performance` 패턴에 standalone `performance` 포함. Korean `성능`은 항상 기술적 의미이지만 English `performance`는 기술/비즈니스/마케팅 모두 가능.
2. 근인 추적: 패턴 `성능|performance|...`에서 `performance` 단독 매칭이 모든 컨텍스트에서 개발자 function 트리거.
3. 구조적 패턴: 다의어 키워드의 독립 매칭은 cross-domain 오탐 유발. Cycle #40의 distance bound와 같은 맥락.
4. 레버리지 포인트: `performance` → compound pattern으로 변경 (기술 컨텍스트 필수). Korean `성능`은 유지.

**[가설]**
- Claim: standalone `performance` 제거하고 compound pattern으로 교체하면 비기술적 오탐 제거
- Basis: "brand performance", "campaign performance" 등 마케팅 컨텍스트에서 오탐
- Prediction: brand strategy 쿼리에서 `performance` function 미탐지, 기술 쿼리는 정상 탐지
- Null: 기술 쿼리에서도 `performance` function 미탐지 (false negative)
- Risk: low — 16개 테스트 케이스 전수 통과
- Impact: Tier 2 에이전트 지능 — 비기술적 "performance" 오탐 제거

**[실험 설계]**
- 실험: `performance` 패턴에서 standalone `performance` 제거, compound 패턴 추가
- 채택: brand 쿼리 → `performance` 미탐지, 기술 쿼리 → `performance` 탐지
- 기각: 기술 쿼리에서 `performance` 미탐지
- 범위: `org/functions.yaml` (1 line changed)
- 되돌림: 가능 — regex만 원복

**[실험 결과]**
- 16개 regex 테스트 케이스 전수 통과 ✅
- A/B 테스트 (서버 배포 후):
  - **Brand query**: `functions=['brand_strategy']` → Ashley Yoo (marketer) ✅ (before: Core + Ashley)
  - **Tech query**: `functions=['performance']` → Core (developer) ✅ (unchanged)
- 변경된 패턴:
  - 제거: standalone `performance`
  - 추가: `(?:code|app|server|api|query|render|page|load).{0,20}performance`
  - 추가: `performance.{0,20}(?:optim|test|bench|tun|profil|issue|bottleneck|improv|monitor|degrad)`
  - Korean `성능` standalone 유지 (기술적 의미 단일)

**[검증] ✅ Confirmed**
- 예측: 비기술적 "performance" 오탐 제거, 기술적 쿼리 정상
- 실제: brand → `performance` 미탐지, API server → `performance` 탐지
- 설명: English "performance"는 다의어. compound pattern으로 기술 컨텍스트 강제하면 오탐 제거.

**[적용]**
- `org/functions.yaml`: `performance` patterns 1줄 변경
  - standalone `performance` 제거
  - compound patterns 추가 (기술 컨텍스트 요구)
- Deployed via SCP, hot-reload 대기 (30s), tested

**[아카이브]**
- auto-zero-2026-02-24.md 업데이트 완료
- auto-zero-summary.md 업데이트 완료
- git commit: pending

→ 다음 가설: 나머지 standalone 영어 키워드 검토 (algorithm, security 등의 다의어 가능성)

---

## Cycle #48 — AI 도메인 regex substring 오탐 수정

**[관찰]**
Cycle #47에서 brand query `brand strategy against competitors performance`의 함수 오탐은 수정했지만, 도메인은 여전히 `['developers', 'marketers']`로 잘못 감지됨. "against"의 "ai" 서브스트링이 `AI` 패턴에 매칭.

**[심층 분석]**
1. 현상 분해: `domains.yaml` 라인 18의 `AI(?!...)` 패턴은 word boundary 없이 `re.IGNORECASE`로 컴파일 → "ai" 서브스트링이 "against", "maintain", "available", "RAIN" 등에서 매칭
2. 근인 추적: `_get_domain_compiled()` (mas_persona_index.py:726)이 `re.IGNORECASE` 사용 → 대소문자 무시하므로 `AI`가 `ai`에도 매칭
3. 구조적 패턴: 짧은 대문자 약어(`AI`, `ML`, `UI`, `UX`)를 IGNORECASE로 매칭하면 영어 단어 내 substring 오탐 발생. `ML`은 이미 `\bML\b`로 보호됨.
4. 레버리지 포인트: `AI` → `\bAI(?=[^a-zA-Z]|$)` — 전방 word boundary + 후방 lookahead로 영어 문자 차단, 한국어 조사(를, 기반) 허용

**[가설]**
Claim: `AI` 패턴에 `\bAI(?=[^a-zA-Z]|$)` word boundary를 추가하면 영어 단어 내 substring 오탐이 해소되면서 한국어 조사 결합도 정상 매칭된다
Basis: Python regex 테스트 15개 케이스 전체 통과
Prediction: brand query → `domains: ['marketers']` (developers 제거), AI 기술 query → `domains: ['developers']` 유지
Null: 변경 후에도 developers 도메인이 잘못 감지되거나 AI 관련 기술 쿼리가 누락
Risk: low — 도메인 키워드 1개만 변경, hot-reload 가능
Impact: 모든 "ai" substring 포함 영어 쿼리의 도메인 분류 정확도 향상

**[실험 설계]**
A/B 테스트:
1. Brand query: `brand strategy against competitors performance` → developers 제거 확인
2. AI query: `AI를 활용한 백엔드 서비스 개발 방법` → developers 유지 확인

**[실험 결과]**
Before: brand query → `domains: ['developers', 'marketers']` (developers 오탐)
After:
- Brand query → `domains: ['marketers']` ✅ (developers 제거됨)
- AI tech query → `domains: ['developers']`, functions: `system_architecture` ✅ (정상)

**[검증]** ✅ Confirmed
예측: brand query에서 developers 도메인 제거, AI 기술 쿼리 유지
실제: 정확히 예측대로
설명: `\bAI(?=[^a-zA-Z]|$)` 패턴이 영어 substring을 완벽 차단하면서 한국어 조사/복합어도 허용

**[적용]**
- `org/domains.yaml` 라인 18: `AI(?!...)` → `\bAI(?=[^a-zA-Z]|$)(?!...)` + 주석 추가
- SCP 배포 (hot-reload, 재시작 불필요)

→ 다음 가설: `training` 키워드 다의어 검토 — "sales training", "training program" 등 비기술 맥락에서 developers 오탐 가능성

---

## Cycle #49 — `training` 키워드 context-bound 수정

**[관찰]**
Cycle #48의 AI 수정 이후, 같은 도메인 패턴의 `training`도 standalone 사용 중. "sales training program for new hires" → `domains: ['developers', 'sales']`, `functions: ['ml_training']` 오탐 확인.

**[심층 분석]**
1. 현상 분해: `training`은 두 곳에서 standalone — domains.yaml (AI 패턴), functions.yaml (ml_training 패턴)
2. 근인 추적: 영어 "training"은 다의어 (ML training vs sales training vs employee training vs strength training)
3. 구조적 패턴: Cycle #47(performance)과 동일한 패턴 — 한국어(학습)는 단의어, 영어(training)는 다의어
4. 레버리지 포인트: `training`을 ML 컨텍스트 워드와 결합: `(model|data|pre).{0,10}training` + `training.{0,10}(data|pipeline|loss|loop|epoch|batch|set|split|run|job)`

**[가설]**
Claim: Standalone `training`을 context-bound 패턴으로 교체하면 비기술 "training" 오탐이 해소된다
Basis: Python regex 테스트 — 5개 비기술 케이스 전부 false positive 확인
Prediction: "sales training" → `domains: ['sales']`, "model training" → `domains: ['developers']`
Null: 변경 후 ML training 맥락이 누락되거나 비기술 training이 여전히 매칭
Risk: low — 패턴 변경만, hot-reload
Impact: sales/CX/HR 관련 쿼리에서 developers 도메인 오탐 완전 제거

**[실험 결과]**
- "sales training program for new hires":
  - Before: `domains: ['developers', 'sales']`, `functions: ['ml_training']`
  - After: `domains: ['sales']`, `functions: ['enterprise_sales']` ✅
- "model training pipeline optimization":
  - After: `domains: ['developers', 'models']`, `functions: ['ml_training', ...]` ✅

**[검증]** ✅ Confirmed
예측: sales training → sales only, model training → developers 유지
실제: 정확히 예측대로

**[적용]**
- `org/domains.yaml`: standalone `training` → context-bound ML patterns
- `org/functions.yaml`: ml_training의 standalone `training` → context-bound ML patterns
- Korean `학습`은 standalone 유지 (ML 맥락에서만 사용되는 단의어)
- SCP 배포 (hot-reload)

→ 다음 가설: 나머지 standalone 영어 키워드 체계적 검토 — 도메인/함수 전체 스캔

---

## Cycle #50 — Standalone 영어 키워드 체계적 스캔 (관찰 사이클)

**[관찰]**
domains.yaml + functions.yaml 전체 스캔 결과:
- 도메인: 115+ standalone 영어 키워드 발견
- 함수: 180+ standalone 영어 키워드 발견

Cross-domain 충돌 테스트 (8개 시나리오):
| Query | Expected | Actual | Issue |
|-------|----------|--------|-------|
| copy the files to server | developers | developers+marketers | copy→marketers |
| browser support for CSS | developers | developers+cx | support→cx |
| smart contract dev | developers | sales only | contract→sales (1위!) |
| deal with the error | developers | sales only | deal→sales (1위!) |
| user account auth | developers | sales only | account→sales (1위!) |
| social media API | developers | developers+marketers | social→marketers |
| video streaming backend | developers | developers+creatives | video→creatives |
| system design patterns | developers | developers (2점) | OK |

**[심층 분석]**
1. 현상 분해: 3가지 심각도 그룹
   - **Critical** (1위 도메인 자체가 잘못됨): deal, account, contract → 영어 전용 기술 쿼리에서 sales 도메인으로 잘못 분류
   - **Moderate** (2위 도메인 오탐): copy, support, social, video → 1위는 맞지만 불필요한 2위 도메인
   - **Safe** (점수 차이로 자연 해소): developers 패턴이 다수 매칭되어 2위 이하로 밀림
2. 근인 추적: 영어 단어 자체가 다의어. 한국어에서는 대부분 전문 용어로만 사용됨
3. 구조적 패턴: MAS 사용자 대부분 한국어 → 영어 전용 쿼리 비율 낮음
4. 리스크 평가: Critical 그룹도 실제 사용 빈도 매우 낮음 (한국어 "계약", "거래" 등은 이미 별도 패턴)

**[판정]** 관찰 기록 — 코드 변경 없음
- 영어 standalone 키워드 다의어 문제는 존재하지만 실제 영향도 낮음
- 한국어 사용자 기반에서 영어 전용 ambiguous 쿼리 빈도 매우 낮음
- 향후 영어 사용자 증가 시 우선순위 상향 가능

→ 다음: regex 분류 정확도 외 다른 레버리지 포인트 탐색 — MAS 로그에서 실제 문제 패턴 관찰

---

## Cycle #51 — Global inference concurrency limiter

**[관찰]**
02:13-02:18 사이 4개 multi-agent 요청 동시 진입 → ~14 에이전트 동시 xapi 호출 → batch timeout → 전원 실패.
MAS에 cross-request 전역 동시 제한(backpressure)이 없어 xapi에 과부하가 전파됨.

**[심층 분석]**
1. 현상 분해: per-request 제한(max_agents=5)만 존재, cross-request 제한 없음
2. 근인 추적: 여러 요청이 동시에 batch 호출 → xapi semaphore(6) 초과 → cascade failure
3. 구조적 패턴: backpressure 부재 — 요청이 무제한 수락되어 downstream 과부하 전파
4. 레버리지 포인트: `threading.Semaphore`로 전역 동시 inference 호출 수 제한

**[가설]**
Claim: `max_concurrent_inferences=3` semaphore를 batch/ThreadPool 경로에 적용하면 burst 시 순차 처리되어 cascade failure 방지
Basis: 02:13 burst에서 14+ 동시 에이전트 → 전원 실패, semaphore 3이면 최대 3 batch 동시 → xapi 부하 관리 가능
Prediction: burst 요청 시 뒤의 요청은 대기 후 순차 처리, 전체 성공률 상승
Null: semaphore 적용 후에도 cascade failure 발생
Risk: low-mid — thread 동기화 추가, race condition 주의
Impact: Tier 1 (신뢰성) — burst 시 전면 장애 → 순차 처리로 부분 성공

**[실험 설계 및 구현]**
- `mas/mas_agent_runner.py`: `threading.Semaphore` + `threading.Lock` (thread-safe init) 추가
- batch 경로: acquire → try/finally(release) 패턴
- ThreadPool 경로: 동일 패턴 적용
- config key: `max_concurrent_inferences` (default 3)

**[실험 결과]**
검증 미완료 — xapi가 외부 세션에 의해 반복 재시작되어 clean 테스트 불가.
코드 배포 + MAS 재시작 완료, 구문 검증 통과.
"Batch acquired" 로그가 출력되지 않는 문제 관찰 — semaphore 코드 경로 도달 여부 추가 조사 필요.

**[검증]** ❓ Inconclusive
예측: burst 요청의 순차 처리 + 성공률 상승
실제: xapi 외부 재시작으로 정상 테스트 불가. 코드 자체는 구문/논리 검증 통과.
다음 행동: xapi 안정 시 자연 검증. "acquired" 로그 미출력 원인 추가 조사.

**[적용]**
- `mas/mas_agent_runner.py`: inference concurrency semaphore 추가

→ 다음: "acquired" 로그 미출력 원인 조사 또는 다른 Tier 1 개선 방향

---

## Cycle #52 — Inference Semaphore 배포 누락 수정 + 검증

**[관찰]**
Cycle #51의 "acquired" 로그 미출력 원인을 조사:
- 서버 파일 (`~/.f1crew/scripts/mas/mas_agent_runner.py`): 901 lines, `import threading` 없음, semaphore 코드 **없음**
- 로컬 소스: 952 lines, semaphore 코드 포함
- 서버 파일 수정 시각: Feb 24 01:06 — Cycle #51 코드 작성 **이전**
- 추가 발견: `journalctl`에 "Batch failed: timed out" 5건 (02:34-02:41) → batch 경로 시도 → timeout → ThreadPool fallback

**[심층 분석]**
1. 현상 분해: Cycle #51에서 코드는 로컬에 커밋했으나 서버 배포가 실제로 이루어지지 않았음
2. 근인 추적: 배포 경로 `~/.f1crew/scripts/mas/` 에 SCP가 실행되지 않았거나 실패함
3. 구조적 패턴: 배포 확인 시 서버 코드 내용을 직접 검증하지 않고 `is-active` 상태만 확인 → 배포 누락 미감지
4. 레버리지 포인트: SCP 배포 후 grep으로 핵심 코드 존재 확인

**[가설]**
Claim: 로컬 mas_agent_runner.py를 서버에 SCP하고 MAS 재시작하면 semaphore "acquired" 로그가 출력된다
Basis: 서버 파일에 semaphore 코드가 물리적으로 없음 (901 vs 952 lines)
Prediction: 배포 후 multi-agent 요청 시 "Batch acquired" 또는 "ThreadPool acquired" 로그 출력
Null: 배포 후에도 "acquired" 로그 미출력
Risk: low
Impact: Tier 1 — Cycle #51 burst protection이 실제로 활성화됨

**[실험 설계]**
- SCP 배포 → 서버 코드 grep 확인 → MAS 재시작 → 테스트 쿼리
- 채택: "acquired" + "Released" 로그 출력
- 기각: 로그 미출력
- 범위: mas_agent_runner.py만
- 되돌림: 가능

**[실험 결과]**
1. SCP 배포 완료, 서버에서 `import threading` + semaphore 코드 확인 ✅
2. MAS 재시작 (active) ✅
3. 테스트: "React와 Vue의 SSR 성능 비교 분석해줘" (request_id: c78c9b74)
   ```
   02:54:43 [c78c9b74] Batch acquired inference slot (2 agents)
   02:56:06 Blaze completed (83043ms, 7682 chars, 23488 tokens, $0.1066)
   02:56:06 Grid completed (72349ms, 6901 chars, 21523 tokens, $0.0905)
   02:56:06 Batch completed: 2 agents in 83056ms
   02:56:06 [c78c9b74] Released inference slot
   ```
   Acquire → 2 agents batch → Release 전체 흐름 정상 ✅

**[검증] ✅ Confirmed**
예측: "Batch acquired" 로그 출력
실제: `Batch acquired inference slot (2 agents)` + `Released inference slot` 정상 출력
설명: Cycle #51의 "Inconclusive" 근본 원인은 **배포 누락**. 서버 파일이 코드 작성 이전 버전이었음.

**[적용]**
- `mas/mas_agent_runner.py` → SCP 배포 (952 lines, semaphore 코드 포함)
- MAS 재시작 완료
- Cycle #51 판정 업데이트: Inconclusive → **배포 누락으로 인한 미검증** (코드 자체는 정상)

**[교훈]**
배포 후 검증 시 `systemctl is-active` 만으로는 불충분. 서버 파일 내용을 `grep`으로 직접 확인해야 함.

→ 다음: batch timeout 원인 조사 (02:34-02:41 5건 실패) 또는 다른 고레버리지 개선점 탐색

---

